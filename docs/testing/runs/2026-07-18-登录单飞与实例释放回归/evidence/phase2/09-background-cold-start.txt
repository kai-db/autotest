# 后台清理/冷启动与实例释放

复用同日运行证据：L1 严格在 `releaseInstanceDuringLogin` 在途窗口执行 force-stop 后冷启动成功；L1 普通冷启 10 轮、L2 普通冷启 1 轮均进入 `MainActivity` 且无 `FATAL EXCEPTION`/`ANR`。完整过程见本目录同名证据及 run17 的 `09-background-cold-start.txt`；普通冷启不替代十轮严格在途用例。
