# 测试用例 — Token 校验误报退出修复 + Zego 超时/线程兜底回归

> 被测增量：上轮已测包（`f09ca3f2`，21400002）之后落库的 4 个 commit，本轮包构建至 dev `ed273d6cd0`：
>
> | commit | 改动 | 测试关注 |
> |---|---|---|
> | `ed273d6c` | **Token 校验网络失败误报退出修复**（TokenVerifyManager 重构 + TokenVerifyRetryPolicy 新增，31 JVM 单测已绿） | P0 主战场：网络失败不污染持久登录态；独立轮次退避重试；网络恢复边沿唤醒；UI 文案 |
> | `559f87b8` | Z-2/Z-3：ZegoRoomRecoveryCoordinator FETCH 阶段 10s 超时兜底 | 防御性改动，正常路径应零行为变化 → 语音房回归 |
> | `3a383cdb` | Z-1：onChatroomJoin 主线程分发防御 | 同上（实测 JIM 回调本在主线程，预期零变化） |
> | `eaada993` | 字号档位收窄 0.9/1.0/1.1/1.2/1.3（上限 1.5→1.3） | 设置页档位数与上限 |

## 项目信息

| 属性 | 值 |
|---|---|
| 测试目标 | DeBox：Token 校验误报退出修复回归 + Zego 防御性修复零回归 + 字号档位 |
| App 包名 | `com.tm.security.wallet` |
| 测试方式 | Claude Code + MCP（AI 驱动黑盒）+ FLogger logcat 证据（tag `Login` / `LiveZego` / `ChatRoom`） |
| 前置条件 | L1 emulator-5554 登录态在；被测包按 TEST_GUIDE §7.7 自主构建至 `ed273d6cd0` 并通过安装预检（I-89） |

---

## 0. 框架前置检查

> 本轮为纯 App 黑盒回归（MCP 驱动，不跑 instrumented），autotest 框架无改动。
> TC-P-001/002/003 沿用上轮（2026-07-11 语音房会话恢复回归）全绿结论，不重复执行；
> 若本轮需要用到 AAR/instrumented 再补跑。

---

## 1. 冒烟测试（P0）

| # | 用例 | 步骤 | 验证标准 | 优先级 | 设备 |
|---|------|------|----------|--------|------|
| TC-S-001 | 被测包就绪（§7.7 全链） | shim 构建 `:app:assembleAppDebug`@`ed273d6c` → apksigner 签名比对 → versionCode ≥ 已装 → `install -r` → 冷启 | 进主页、登录态在、logcat 无 FATAL；归因链落档（SHA/sha256/versionCode）；行为探针：dex 含 `TokenVerifyRetryPolicy` + `Login` tag 新埋点出现 | P0 | L1 5554 |
| TC-S-002 | 在线冷启 Token 校验基线 | 正常网络冷启 → 观察 `Login` tag 埋点与 UI | 校验成功链路埋点正常；**无**「DID已退出登录」弹窗/踢登录页；IM 在线 | P0 | L1 5554 |
| TC-S-003 | 语音房建房冒烟（Z-1/Z-2/Z-3 零回归） | 建房 → 观察 `LiveZego` 状态机 → 正常关房 | `Idle→LoggingIn→LoggedIn` gen=1、`login_ok`、零 `1002xxx`；关房 `LoggingOut→Idle` 干净；无 recovery timeout 误触发 | P0 | L1 5554 |

---

## 2. 功能测试（P1）

| # | 用例 | 步骤 | 验证标准 | 优先级 | 设备 |
|---|------|------|----------|--------|------|
| TC-F-001 | 断网冷启不误登出 | `svc wifi disable`+`svc data disable` → 杀进程冷启 | 保留登录态（不弹 DID 退出、不进登录页）；网络失败**不落持久化**（埋点示 GET_VALIDA_FAILED 不写 Realm，保留上次有效状态）；UI 显示网络态提示而非登出 | P1 | L1 5554 |
| TC-F-002 | 断网退避重试独立轮次 | 断网保持 ≥90s，观察 `Login` 埋点 | 重试按轮进行（6 次/轮）、退避间隔递增（30s 起）；期间不弹退出登录、不写脏状态 | P1 | L1 5554 |
| TC-F-003 | 网络恢复边沿唤醒自动恢复 | 恢复 wifi/data → 不做任何手动操作 | 网络 VALIDATED 边沿自动触发校验 → 校验成功、IM 自动重连；全程零人工 | P1 | L1 5554 |
| TC-F-004 | 关房→再建房幂等（Z 回归） | 建房→关房→立即再建房 | gen 换代正确（n→n+1）、零 `1002001`、无泄漏 pending | P1 | L1 5554 |
| TC-F-005 | 房内断网→恢复受控重进（Z-2/Z-3 正常路径） | 房内 `svc` 断网 ~20s → 恢复 | recovery 链 FETCH 正常完成（10s 内回包则**不**触发 phaseTimeout）；恢复后回 LoggedIn；若 fetch 失败必收敛 `quit→reset→Idle` 不卡 ReconnectFailed | P1 | L1 5554 |
| TC-F-006 | 字号档位收窄 | 设置 → 字号页 | 档位恰为 0.9/1.0/1.1/1.2/1.3 共 5 档（无 1.4/1.5）；设 1.3 后 UI 正常渲染、重启后保持 | P1 | L1 5554 |

---

## 3. 稳定性/边界测试（P2）

| # | 用例 | 步骤 | 验证标准 | 优先级 | 设备 |
|---|------|------|----------|--------|------|
| TC-T-001 | 断网杀进程→断网冷启→恢复 | 断网 → 杀进程 → 断网中冷启 → 30s 后恢复网络 | 登录态全程不丢；恢复后自动校验成功；无 FATAL | P2 | L1 5554 |
| TC-T-002 | 反复断网/恢复 ×3 | 断网↔恢复循环 3 次（每态 ≥20s） | 轮次隔离正确（stale 轮零副作用）、无重复弹窗堆积、无 FATAL、最终态在线且登录态在 | P2 | L1 5554 |

---

## 危险操作边界（铁律#7）

- 全程**禁止**：登出/清数据/卸载/切环境（TC-F-001 断网仅用 `svc` 命令，绝不碰 App 内退出登录入口）。
- 字号设置页如出现账号级危险入口一律规避。
- 测试完成后恢复字号 1.0、网络全开。

## 统计

| 分类 | 用例数 | P0 | P1 | P2 |
|------|--------|----|----|----|
| 框架前置检查 | 0（沿用上轮全绿） | 0 | 0 | 0 |
| 冒烟测试 | 3 | 3 | 0 | 0 |
| 功能测试 | 6 | 0 | 6 | 0 |
| 稳定性/边界 | 2 | 0 | 0 | 2 |
| **合计** | **11** | **3** | **6** | **2** |
