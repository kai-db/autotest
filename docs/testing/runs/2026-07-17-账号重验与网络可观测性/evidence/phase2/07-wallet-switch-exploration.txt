执行时间：2026-07-18 15:40-15:47（Asia/Singapore）

UI 探索（不替代完整 D-004）
- L1：从 `10b92305` 打开“切换账号”弹层，点击新鲜 layout 定位的本地钱包行“哈哈”（未碰行尾编辑）；页面切换到“哈哈”，
  日志出现 -2018 episode/auto_resign，约 6s 后 `app_login ok channel=JC`；随后按新鲜 layout 切回 `10b92305`。
- L2：从 `2309b9ea` 点击同列表本地钱包行“哈哈”，页面切换成功，但 50s 内仅出现 `login_out`，未出现 `app_login ok`；随后切回 `2309b9ea`，页面恢复测试账号。
- 两机均未访问账号详情、删除、导出、签名、资金或环境入口；L1/L2 当前前台分别恢复 `10b92305`/`2309b9ea`。

归因：该探索证明切换入口和恢复路径可达，但没有制造“登录中延迟后切钱包”的旧回包窗口，且 L2 目标钱包未在观察窗内登录成功，因此不把它记作 D-004 PASS。

