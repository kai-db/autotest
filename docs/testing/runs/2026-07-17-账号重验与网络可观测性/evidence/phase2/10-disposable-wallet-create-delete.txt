# disposable 钱包创建与删除

日期：2026-07-18，设备 L1

- 从已备份账号进入“切换账号 → 添加 → 创建钱包”。
- 使用用户提供的 PIN `123456` 完成创建；创建成功页选择“跳过”备份，不查看、不记录助记词或私钥。
- 新建钱包显示为临时 `Wallet`，无资产；重进账号列表可见，说明本地创建与持久化成功。
- 进入该临时钱包“账号详情 → 移除并注销账号”，勾选三项安全确认后确认删除；未触碰任何原有备份钱包。
- 删除后账号列表不再显示该临时钱包，当前页面短暂显示 `Empty`；随后重新选择并恢复原账号 `10b92305`。
- 最终前台账号恢复为原备份账号，未读取敏感材料。
