# 开发任务清单

> 基于技术方案拆解，共 14 条任务，按依赖顺序排列。

## 飞书多维表格导入格式

> 复制下方表格内容（含表头），粘贴到飞书多维表格即可自动分列。

```
任务编号	任务名称	描述	产出物	阶段	优先级	依赖任务	状态
T-01	项目初始化 + 构建配置	创建 Android Library 模块 autotest，配置 Kotlin 2.0、AGP 8.7、Espresso 3.5.1、UiAutomator 2.3.0 依赖，配通 publishToMavenLocal	build.gradle.kts + mavenLocal 发布可用	阶段1-框架基础	P0	无	未开始
T-02	测试基类 + 配置系统	实现 BaseUiTest、BaseActivityTest 基类；实现 TestConfig 分层配置（命令行 > properties > 环境默认值）+ Environment 多环境管理	base/ 和 config/ 模块	阶段1-框架基础	P0	T-01	未开始
T-03	工具扩展层	Espresso 扩展（waitForView、clickWithRetry）、UiAutomator 扩展（clickText、waitForText）、WaitUtil 条件等待、ScreenshotRule 截图规则	util/ 模块	阶段1-框架基础	P0	T-02	未开始
T-04	通用操作与断言	AppActions（Tab 切换、引导页跳过、弹窗处理）、AppAssertions（前台断言、文本可见、控件存在）、DeviceActions（网络控制、权限管理、屏幕解锁、Logcat 收集）	action/ + assertion/ + device/ 模块	阶段1-框架基础	P0	T-03	未开始
T-05	DSL 步骤体系	实现 Scenario、Step DSL（scenario/step 链式调用）+ BaseScenario 可复用场景基类	dsl/ 模块	阶段2-DSL与引擎	P0	T-03	未开始
T-06	拦截器链	InterceptorChain + 5 种内置拦截器：DialogDismiss（弹窗自动关闭）、Logging、Screenshot（失败截图）、Performance（耗时警告）、Logcat（失败日志）	intercept/ 模块	阶段2-DSL与引擎	P0	T-05	未开始
T-07	生命周期 + 日志	TestLifecycleHook/Manager（before/after/onFailure 钩子）；TestLogger/DefaultTestLogger（分级日志 + 文件输出 + Logcat 输出）	lifecycle/ + log/ 模块	阶段2-DSL与引擎	P1	T-06	未开始
T-08	执行引擎	TestRunner（环境重置→前置→Scenario→后置→收集结果）、TestSuite（批量管理 + P0→P1→P2 排序）、TestCaseParser（从 Markdown 解析用例）	engine/ 模块	阶段2-DSL与引擎	P0	T-07	未开始
T-09	结构化报告	RunReport 数据结构、ReportWriter（JSON）、HtmlReporter（HTML）、ReportSummary/ReportCollector（统计汇总）；集成 RunnerInfo + DeviceSelector 设备信息	report/ + runner/ 模块	阶段3-报告与稳定性	P1	T-08	未开始
T-10	稳定性治理	FlakyClassifier（自动识别 Flaky 用例）、RetryRunner + RetryPolicy（失败自动重试）、WaitPolicy（等待策略）	stability/ 模块	阶段3-报告与稳定性	P1	T-09	未开始
T-11	单元测试全覆盖	为所有模块编写单元测试，目标 85+ 条测试用例，覆盖核心逻辑分支	src/test/ 下 19 个测试文件	阶段3-报告与稳定性	P1	T-10	未开始
T-12	mobile-mcp 对接验证	配置 mobile-mcp Server，验证 Claude Code 通过 MCP 连接真机（list_devices → launch_app → take_screenshot），确保 10 个 MCP 工具全部可用	MCP 配置 + 连接验证通过	阶段4-AI测试闭环	P0	T-01	未开始
T-13	测试文档体系	编写 TEST_GUIDE.md（铁律+流程+修复原则）、TEST_CASES.md（框架 3 条 + DeBox 15 条）、TEST_RESULTS.md（结果模板）、runs/ 目录结构	docs/testing/ 完整文档	阶段4-AI测试闭环	P1	T-12	未开始
T-14	AI 监工模式	实现 MonitorMode：Phase 1 环境确认 → Phase 2 全量测试 → Phase 3 结果记录 → Phase 4 修复 → Phase 5 回归 → Phase 6 验收；集成 /loop 5m 看护机制	engine/MonitorMode + 闭环可用	阶段4-AI测试闭环	P0	T-08, T-13	未开始
```

## 依赖关系

```
T-01 → T-02 → T-03 → T-04
                 ↓
T-05 → T-06 → T-07 → T-08 → T-09 → T-10
                                       ↓
                                     T-11
                                       ↓
                              T-12 → T-13 → T-14
```

## 里程碑

| 里程碑 | 完成任务 | 标志 |
|--------|----------|------|
| M1: 框架可用 | T-01 ~ T-04 | publishToMavenLocal 成功，基础 API 可调用 |
| M2: 引擎可用 | T-05 ~ T-08 | 能用 DSL 写用例并通过引擎执行 |
| M3: 生产就绪 | T-09 ~ T-11 | 报告输出 + Flaky 治理 + 单测全绿 |
| M4: AI 驱动测试 | T-12 ~ T-14 | Claude Code 监工模式跑通全量 DeBox 用例 |
