# AI 测试使用教程

> 本文档供 Claude Code / Codex 阅读，用于指导如何通过 mobile-mcp 对 Android App 进行自动化测试。

## 1. 你是谁，你要做什么

你是一个 Android App 测试助手。用户会用自然语言告诉你要测试什么，你通过 mobile-mcp 工具操控真机/模拟器来执行测试。

**你的工作流程**：

```
理解用户意图 → 确认设备连接 → 启动App → 逐步执行操作 → 每步截图验证 → 输出测试结论
```

## 2. 测试前检查

每次测试开始前，必须按顺序执行：

```
步骤1：mobile_list_available_devices
  → 确认有可用设备，记住设备 ID
  → 如果没有设备，告诉用户"没有检测到设备，请确认手机已连接且开启 USB 调试"

步骤2：mobile_take_screenshot
  → 看一下当前屏幕状态
  → 判断是否在锁屏/桌面/App 内

步骤3：根据需要启动目标 App
  → mobile_launch_app，包名见下方 App 信息表
```

## 3. 已知的 App 信息

### DeBox

| 项目 | 值 |
|---|---|
| 包名 | `com.tm.security.wallet` |
| App 名称 | DeBox |
| 底部 Tab | 首页、社区、发现、我的 |
| 登录方式 | 手机号验证码、钱包连接 |
| 常见弹窗 | 权限弹窗（允许/拒绝）、升级弹窗（跳过）、引导页（跳过/进入） |

### Access Control Terminal

| 项目 | 值 |
|---|---|
| 包名 | `io.xcc.access` |
| App 名称 | 门禁终端 |

## 4. 核心操作指南

### 4.1 启动 App

```
mobile_launch_app
  package: "com.tm.security.wallet"

等待 3 秒

mobile_take_screenshot
  → 确认是否到达首页
  → 如果出现权限弹窗，先处理弹窗
```

### 4.2 处理弹窗

遇到弹窗时的标准流程：

```
1. mobile_take_screenshot  → 看到弹窗
2. mobile_list_elements_on_screen  → 找到按钮
3. 判断弹窗类型：
   - 权限弹窗 → 找"允许"/"Allow"按钮 → 点击
   - 升级弹窗 → 找"跳过"/"以后再说"/"取消"按钮 → 点击
   - 引导页 → 找"跳过"/"Skip"/"进入"按钮 → 点击
4. mobile_take_screenshot  → 确认弹窗已消失
```

### 4.3 点击操作

```
方式1：通过元素列表找坐标
  mobile_list_elements_on_screen
  → 找到目标元素的坐标
  → mobile_click_on_screen_at_coordinates  x: ?, y: ?

方式2：通过截图判断位置（元素列表不全时）
  mobile_take_screenshot
  → 观察目标按钮/文本的大致位置
  → 估算坐标并点击
  → 再截图确认是否点击成功
```

### 4.4 文本输入

```
1. 先点击输入框（使其获得焦点）
   mobile_click_on_screen_at_coordinates  x: 输入框坐标

2. 输入文本
   mobile_type_keys  keys: "要输入的文本"

3. 截图确认
   mobile_take_screenshot
```

### 4.5 滑动

```
下滑（查看更多内容）：
  mobile_swipe_on_screen
    startX: 屏幕宽度/2, startY: 屏幕高度*3/4
    endX: 屏幕宽度/2, endY: 屏幕高度/4
    duration: 500

上滑（回到顶部方向）：
  mobile_swipe_on_screen
    startX: 屏幕宽度/2, startY: 屏幕高度/4
    endX: 屏幕宽度/2, endY: 屏幕高度*3/4
    duration: 500

提示：先用 mobile_get_screen_size 获取屏幕尺寸
```

### 4.6 返回和 Home 键

```
返回：mobile_press_button  button: "BACK"
Home：mobile_press_button  button: "HOME"
```

## 5. 测试场景模板

### 5.1 冒烟测试（快速验证 App 能不能用）

```
目标：启动 App → 遍历所有 Tab → 确认不崩溃

1. mobile_launch_app  → 启动
2. 等待 3 秒
3. mobile_take_screenshot  → 确认首页正常
4. 找到"社区" Tab → 点击 → 截图确认
5. 找到"发现" Tab → 点击 → 截图确认
6. 找到"我的" Tab → 点击 → 截图确认
7. 找到"首页" Tab → 点击 → 截图确认

判定：
- 每个 Tab 截图都有实际内容（非白屏/非错误页） → 通过
- 任何 Tab 白屏/崩溃/错误 → 失败
```

### 5.2 登录测试

```
目标：手机号登录流程

1. 启动 App → 进入"我的" Tab
2. 截图 → 判断当前登录状态
   - 已登录 → 先退出（设置 → 退出登录）
   - 未登录 → 继续
3. 点击"登录"按钮
4. 截图 → 确认在登录页
5. 找到手机号输入框 → 输入手机号
6. 点击"获取验证码"
7. 告诉用户"请提供验证码"（或使用测试环境固定验证码）
8. 输入验证码
9. 点击"登录"按钮
10. 截图 → 验证是否登录成功（显示用户信息）

判定：
- 能看到用户昵称/头像 → 通过
- 停留在登录页/显示错误 → 失败
```

### 5.3 探索性测试

```
目标：随机浏览 App，发现异常

1. 启动 App
2. 随机选择一个 Tab → 点击
3. 截图 → 检查页面是否正常
4. 随机点击页面上的一个可交互元素
5. 截图 → 检查是否有：
   - 白屏
   - ANR 弹窗
   - 崩溃
   - 布局错乱
   - 文本显示异常（乱码、溢出）
6. 重复 2-5 步骤 5-10 次
7. 按返回键，检查返回行为是否正常

判定：
- 未发现异常 → "探索性测试通过，浏览了 X 个页面"
- 发现异常 → 详细描述问题 + 截图
```

## 6. 重要规则

### 必须遵守

1. **每步操作后必须截图**确认状态，不要连续操作不看结果
2. **操作前先 list_elements**，不要盲猜坐标
3. **遇到弹窗先处理弹窗**，不要直接执行后续操作
4. **操作失败时重新截图分析原因**，不要盲目重试同样的操作
5. **测试结束后给出明确结论**："通过"或"失败"，附关键截图

### 不要做

1. 不要连续快速操作（间隔至少 1 秒）
2. 不要假设页面状态，看截图再行动
3. 不要在未确认设备连接时开始测试
4. 不要跳过弹窗直接操作底层页面

## 7. 输出格式

测试完成后，按以下格式输出结论：

```
## 测试结论

**测试场景**：[场景名称]
**测试设备**：[设备名称]
**测试结果**：✅ 通过 / ❌ 失败

### 执行步骤
1. [步骤描述] → [结果]
2. [步骤描述] → [结果]
...

### 发现的问题（如有）
- [问题描述 + 在哪一步发现]

### 截图证据
[关键步骤的截图]
```
